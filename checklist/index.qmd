---
title: "Minimum Reporting Checklist"
subtitle: "Ten methods-section disclosures about the measuring system."
toc: true
---

Clinical trials have CONSORT. Systematic reviews have PRISMA. Observational
studies have STROBE. For in-the-wild sensing there is no agreed statement of
what a study should have done and reported for its measurements to be assessable
by a reader.

The MRC reports a **different object** than existing mHealth reporting
standards. CONSORT-EHEALTH and mERA standardize the reporting of
*interventions*, saying little about the acquisition system that produced the
sensor data. ACQUIRE is complementary: it reports the **measuring system**.

::: {.callout-important appearance="simple"}
**v0.1 is one group's opinion, not a community consensus.** The checklist has
not been evaluated for usability, coverage, or inter-rater agreement, and no
claim is made that applying it improves study outcomes.

It becomes a standard only if other groups use it, disagree with it in public,
and shape it. [Challenge an item →](https://github.com/acquire-framework/acquire-framework.github.io/issues/new?template=checklist-feedback.yml)
:::

## Who it is for

**Authors**, who attach it to a methods section. **Reviewers**, who use it to
request specific acquisition details rather than a vague "more detail on data
quality" — see the [reviewer form](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/reviewer-form.md).
**Dataset curators**, who use the schema and provenance templates. And
**students**, for whom acquisition discipline is most cheaply instilled during
supervision.

## The checklist

```{python}
#| echo: false
import yaml
from pathlib import Path
from IPython.display import Markdown

mrc = yaml.safe_load(Path("checklist/acquire-mrc.yml").read_text())

AUTO = {
    True: "automatic",
    "partial": "partly automatic",
    False: "author-disclosed",
}

out = ["| | Disclose in the methods section | Property | Tier | Disclosure |",
       "|:--|:--|:-:|:-:|:-:|"]
for item in mrc["items"]:
    props = " ".join(f"[{p}]" for p in item["property"])
    out.append(
        f"| `{item['id']}` | {item['statement'].strip()} | {props} "
        f"| {item['tier']} | {AUTO[item.get('auto', False)]} |"
    )
Markdown("\n".join(out))
```

## Proportionality

Items marked **core** apply to any study using this framework. Items marked
**conditional** should be judged against the intended measurand and the study's
risk, not applied uniformly.

The synchronization example makes the point concretely: tens of milliseconds of
clock error are negligible for daily step counts and disqualifying for
cross-modal latency estimation. **The same dataset can be fit for one purpose
and unfit for another.** Acquisition requirements should be set and evaluated
against what is being measured.

## Disclosure should be a by-product, not extra work

The reason most acquisition detail goes unreported is not that authors are
careless — it is that disclosure is unpaid work with no reward, and often
requires numbers nobody computed.

Six of the ten items can be populated wholly or partly from data and logs by the
shipped diagnostics, so that reporting becomes a by-product of a validation you
would want to run anyway:

```bash
pip install acquire-framework
acquire check recordings/day01.csv --nominal 50
```

```{python}
#| echo: false
out = ["| Item | What the diagnostics supply | What you must still supply |",
       "|:--|:--|:--|"]
for item in mrc["items"]:
    if item.get("auto") in (True, "partial"):
        note = item.get("auto_note", "").strip()
        supplied, _, manual = note.partition(" ")
        out.append(f"| `{item['id']}` | {note} | — |")
Markdown("\n".join(out))
```

The remaining four items — integrity logic, health-event recording, published
schema and software, and pre-deployment verification — are properties of how the
study was built. No tool can infer them, and the checklist's role there is to
make their absence visible rather than silent.

## A floor under item MRC-05

MRC-05 permits a documented justification in place of an uncertainty statement.
Without a floor, that escape hatch would be taken universally and the item would
carry no information. A justification is acceptable only if it:

1. **Names the dominant uncertainty sources** that would appear in a budget
2. **States why each could not be quantified**
3. **Describes the validation evidence offered instead**

"Uncertainty could not be established" alone does not satisfy the item. The
[uncertainty statement template](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/uncertainty-statement.md)
provides both the budget form and the justification form.

## Templates

| Template | Satisfies |
|:--|:--|
| [Sensor schema](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/sensor-schema.yml) | MRC-08 |
| [Quality flags](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/quality-flags.yml) | MRC-01, MRC-02, MRC-09 |
| [Uncertainty statement](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/uncertainty-statement.md) | MRC-05 |
| [Dataset provenance](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/dataset-provenance.yml) | MRC-03, MRC-09 |
| [Reviewer form](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/templates/reviewer-form.md) | all |

## Going further

The MRC is the reporting minimum — what a reader needs to assess a study after
the fact. The [extended operational checklist](extended.qmd) is a longer,
stage-by-stage companion for people **building** a study, covering decisions
that never appear in a methods section but determine whether the MRC can be
answered honestly at all.

## Source

[`checklist/acquire-mrc.yml`](https://github.com/acquire-framework/acquire-framework.github.io/blob/main/checklist/acquire-mrc.yml)
is the single source of truth: the site renders it, the exports derive from it,
and each item names the taxonomy rows it comes from.
